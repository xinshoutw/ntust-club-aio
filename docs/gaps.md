# 未完成功能

以「這件事現在能不能做」為準。逐頁細節見 [spec/](spec/);已做但做錯的見 [issues.md](issues.md)。

## 1. 有入口、沒有實作

| 編號 | 功能 | 現況 | 影響 |
|---|---|---|---|
| GAP-01 | 分組與評審指派 | `eval_groups` / `eval_group_clubs` / `eval_group_reviewers` 三張表存在,**沒有任何寫入 API,行政端也沒有頁面**。只能直接操作 DB | 評審端三頁在正式環境永遠是「尚未被指派評分」 |
| GAP-02 | 評審代號 A/B | `eval_group_reviewers.sort` 有欄位,匿名映射未實作;`sort` 也無唯一性保證 | 社團看評語時無從區分評審,代號會漂移 |
| GAP-03 | 競賽成績總表 | 跨評審平均、最佳社團獎 40%/60% 加權、名次全部沒有實作。`services/evaluation.py` 只有行政分自動計算 | 評鑑跑完也算不出成績 |
| GAP-04 | 評鑑結果頁 | 頁面、路由與側欄入口皆已移除(原本硬編假分數);設計意圖留在 [spec/club/eval-result.md](spec/club/eval-result.md) | 社團看不到自己的成績與評語 |

> GAP-01 → 02 → 03 → 04 是同一條彙總鏈,應當作**單一開發段落**規劃,不要拆散。是否要做取決於「這學年評鑑要不要在新系統跑」。

## 2. 有 API、沒有 UI(或反之)

| 編號 | 功能 | 現況 |
|---|---|---|
| GAP-05 | 場地主檔 CRUD | 器材主檔有完整 CRUD([spec/admin/settings.md](spec/admin/settings.md)),**場地沒有**。新增或停用任一場地都要改 seed 並重新部署 |
| GAP-06 | 政府行事曆假日匯入 | `holidays` 表**有讀無寫**,沒有匯入介面。未匯入年度的器材逾期判定退化成只排除週六日 |
| GAP-07 | 逐年 rubric 與獎項主檔管理 | `award_rubric_items` 綁 `(award, year)`,設計上新學年複製上年再修改,**介面未做**。117 學年度只能改 code |
| GAP-08 | 檔案歸檔 | `files.archived_at` 的語意、下載 410、不計配額都實作了,**沒有任何地方可以設這個欄位**。磁碟只增不減 |
| GAP-08b | 評鑑上傳鎖 / 評語公布 | `eval_settings.unlocked` 與 `comment_released` 全後端**只有讀、沒有任何寫入端點或 UI**。要凍結某獎項的資料上傳只能直接動 DB |
| GAP-10 | 報名活動修改 / 關閉 | 只有 `POST`,沒有 `PUT`/`PATCH`/`DELETE`。打錯字或要提前截止只能動 DB |
| GAP-11 | 社團性質(自治性/學藝性…)維護 | 是公告分眾與統計的依據,**API 與 UI 都改不了** |

## 3. 完全沒做的功能

| 編號 | 功能 | 說明 |
|---|---|---|
| GAP-12 | 逾時待審自動駁回 | pending 佇列會無限累積 |
| GAP-13 | 批次核准 / 衝突連鎖自動駁回 | 固定借用衝突時要逐單開彈窗處理,承辦工時倍增 |
| GAP-14 | 統計與匯出 | 社團活動統計、競賽資料完成度、經費稽核明細、xlsx 匯出全部沒做 |
| GAP-15 | 待審申請彙整 | 現行「線上申請管理」只併了幹部證明與郵局異動兩個端點,報修、借用、活動都不在其中 |
| GAP-16 | 社團導覽首頁 | Roadmap 項目:`/` 現在直接是登入頁 |
| GAP-17 | 公開頁 | 舊系統的公開頁三件套退役後直接 404(網域是復用的) |
| GAP-18 | 通知覆蓋 | 有 7 類動作目前完全不發通知;行政/工讀生/評審端的鈴鐺永遠是空的 |
| GAP-19 | 評分草稿跨裝置 | 評審評分只存在元件狀態,關窗即失 |

## 4. 維運與部署

| 編號 | 項目 | 現況 |
|---|---|---|
| OPS-01 | 備份機制 | **完全沒有**,且每次部署自動跑 migration 沒有前置備份 |
| OPS-02 | 環境防護 | `reset_db.py --yes` / `seed_mock.py --yes` 無任何環境檢查,可對正式庫 `DROP SCHEMA` 並清空上傳目錄 |
| OPS-04 | 正式 seed 缺器材主檔 | 遷移的 `DEVICE_RENAME` 會落空,序號點交功能全失效 |
| OPS-05 | Edge proxy 切換 | 六個項目仍是原狀(upstream 埠號、`client_max_body_size`、`proxy_request_buffering`、XFF 覆寫、`FORWARDED_ALLOW_IPS`、clubclass 導向) |
| OPS-06 | SMTP relay | 未定,現用開發者個人信箱 |
| OPS-07 | 容量告警 | 「已接受的磁碟 DoS 風險」的前提條件至今未建立 |
| OPS-08 | CI | 不跑前端 lint 與 test;映像只 tag `latest`,無法回滾到特定版本 |
| OPS-09 | migration 測試 | 17 個 Alembic revision 完全未被測試,測試用 `create_all` 繞過遷移鏈 |
| OPS-10 | 遷移腳本測試 | 兩套舊系統匯入腳本零測試 |

## 5. 資料遷移待決

| 編號 | 待決事項 |
|---|---|
| MIG-01 | 舊機 media 目錄(約 8.7 萬檔)何時交付 |
| MIG-02 | 舊評鑑檔案庫(12,752 檔 / 14 分類)與行政歷史文件要不要遷 |
| MIG-03 | clubclass 有 960 筆器材借用(11.7%)因 `club_id` 為空字串被靜默丟棄,其實可還原 |
| MIG-04 | 換新 dump 重跑遷移會撞唯一約束,前面幾萬筆全白做 |
| MIG-05 | 遷入後 159 社的聯絡 Email 全為空,公告 Email 通知寄給 0 個收件人 |
| MIG-06 | 約 560 筆借用以「`club_id` 開頭為 8」被歸為學務處,這 5 組帳號身分未經確認 |
| MIG-07 | clubclass 與 CMS 的退役順序 —— 若 clubclass 較晚退役,登入會直接斷 |

## 6. 需要承辦決定才能排程

| 編號 | 問題 | 影響 |
|---|---|---|
| DEC-01 | **這學年的社團評鑑要不要在新系統上跑** | 決定 §1 整條彙總鏈是不是硬阻擋,牽動整個上線排程 |
| DEC-02 | 現場簡報 20 分可否於簡報後補登 | 現行實作是「可以」 |
| DEC-03 | 固定借用開放窗結束後,承辦還要不要能審已收到的申請 | 現行實作是「不能」(整頁鎖) |
| DEC-04 | 器材核准時「可借數不足」要不要硬性擋 | 現行實作是「不擋,只紅字警示」 |
| DEC-05 | `aclose` 權限鍵是否應涵蓋「結案核准」動作 | 現行只持 `aclose` 的帳號進得了頁面但按不了核准 |
| DEC-06 | 郵局帳戶異動的三對互斥組合是否正確 | 現行是實作端訂的 |
| DEC-07 | 實際到場但未線上報名的社團可否補登簽到 | 現行實作是「不行」 |
| DEC-08 | 行政分總分有無下限 | 現行可為負數 |
| DEC-09 | 承辦人核定金額可否高於社團「擬請補助」 | 現行 API 完全不擋 |
| DEC-10 | 最佳活動獎 rubric 合計 90,與該 PDF 自述「競賽資料 100%」矛盾 | 需確認以哪份為準 |
| DEC-11 | 逾期提醒信要不要自動排程 | 現行全靠人工按鈕 |
| DEC-12 | 舊 clubclass 全站雙語,新系統明文不做 i18n | 需確認可接受 |
