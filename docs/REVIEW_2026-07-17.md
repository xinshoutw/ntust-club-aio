# 交叉審查 findings(2026-07-17 第十輪 changeset)— 已全數處理

> 對 `61996d4..HEAD`(dev,40 commit / 73 檔)做的對抗式跨模型審查。
> **2026-07-17 修復 session 已逐項處理完畢**(需求方拍板:P1-4 採「自動歸屬下一學期」、範圍外兩項一併修)。
> 每項標注處理結果與 commit;P3-5 實測不成立,不修。全部完成後已重跑全套驗證(見文末)。

## 審查方式

四個審查者平行對抗式檢查(比例符合 AGENTS「opus 為主、codex 為輔」):

- opus ×1 — 後端儲存/上傳(files.py 配額、admin_files、三處上傳端點、settings、club_config)
- opus ×1 — 後端借用場況 / 活動草稿結案 / 器材主檔 + migration
- opus ×1 — 前端(活動申請/結案、clubConfig、admin 設定/器材、TakeoverOverlay、場況)
- codex(gpt-5.6-sol xhigh)×1 — 三處最高風險跨切面(儲存配額、結案照片回滾、部分草稿 CHECK)

---

## P1 — 已修復

### 1. 器材主檔「總數」欄每個按鍵送一次 PATCH〔前端 HIGH〕✅ `2dca6ae`
`AdminEquipmentCard.tsx` InputNumber onChange 直接 PATCH,回應順序顛倒會停在中間值。
**處理**:總數欄改本地 state + onBlur diff 提交(比照名稱欄);名稱/總數本地草稿隨 refetch 同步、清空回權威值(附帶收掉「本地 state 不隨 refetch 同步、併發改名互蓋」LOW)。

### 2. 結案送出 vs 刪照片跨端點競態 + `/close` 未驗證照片〔codex HIGH〕✅ `5ffd0de`
**處理**:`submit_close`/`upload_photo`/`delete_photo` 統一「get_own_activity → `db.refresh(attribute_names=["status"], with_for_update=True)` → 檢核」鎖序;close 先 commit 後,刪除/上傳看到 closing → 409。`submit_close` 取鎖後計數 `report_photo` slot 未歸檔張數,0 張回 422(照片檢核收口到後端)。測試:零照片 422、closing 後刪照片 409;既有結案測試補照片上傳。

### 3. 送出失敗 + 重載後孤兒照片無回收路徑〔codex HIGH / 前端 MEDIUM〕✅ `9629d3f`
**處理**:結案表單載入 `detail.photos`(APPROVED 下即前次失敗殘留)為**可移除的既有照片**;張數/加總/去重皆併計 existing + 新選;後端 `FileOut` 加 `sha256`,前端對兩集合做內容去重。使用者可見孤兒並刪除回收 10MB 額度。

### 4. 固定借用場況「永久佔格」〔後端 HIGH〕✅ `7d64472`(需求方拍板:自動歸屬下一學期)
**處理**:`RoomBookingRequest` 加 `start_date`/`end_date` 目標學期快照(migration `6b5a9c3affd0`,既有列依 created_at 台北時區回填);申請時 `next_semester_range(today)` 推導(6 月窗→8/1–1/31、1 月窗→2/1–7/31);club/admin 場況只在區間內標格;每社 10 節額度改「同目標學期未退回單」合計。附帶:availability-range 加 venue 參數 + weekday/venue 條件下推 SQL,前端單場地檢視帶 venue。seed_mock 的固定借用用當前學期起訖(展示資料今天可見)。

---

## P2 — 已修復

### 5. 批次選結案照片時 `photosRef` 可能讀過期值〔前端 MEDIUM〕✅ `4df868f`
**處理**:照抄 `AttachmentArea` eager-ref 模式(`commitPhotos` 同步 ref 再 setState),佇列任務不受 render 時序影響。

### 6. `budget_categories` 舊字串格式 → 500〔後端 LOW〕✅ `ea7d4ae`
**處理**:`settings_service.get_budget_categories()` 統一正規化(str → `{name, hint:""}`),activities 校驗、`/club/config`、`/admin/settings` GET 三個讀取端全部走它。測試:殘留舊格式建申請 201、config/settings 回新形狀。

---

## P3 — 已修復(除實測不成立者)

- **admin_files 契約語義**✅ `cb5e229`:`capacity`/`remaining` 改名 `disk_total`/`disk_free` + 註解更新;前端佔用條加「其他佔用(OS/同機程式)」段,各段對磁碟總量的比例正確,尾段「可用」= 實際 disk_free。
- **test_admin_files 脆化**✅ `cb5e229`:monkeypatch `file_service.shutil.disk_usage` 給定值,斷言精確(不再依宿主磁碟)。
- **postcheck unlink 失敗把 413 轉 500**✅ `35f1d7a`:`file_service.unlink_quiet()`(吞 OSError + log)套用所有清理性 unlink(超額回滾、dedup 回滾、commit 後清理),不再讓清理錯誤蓋掉主要回應。
- **`date` 欄位名遮蔽 datetime.date**✅ `f3d951b`:`VenueBooking`/`Holiday` 所在兩檔改 `import datetime as dt` + `dt.date` 別名(與 activities 一致)。
- **migration downgrade CHECK 不保證發出**✳️ **實測不成立,不修**:對 dev 庫實際 `alembic downgrade 33e4dcd04463→ccb5bc27926e` 後查 pg_constraint,`ck_equipment_equipment_category` CHECK **有**被自動建出(本專案鎖定的 Alembic/SQLAlchemy 版本會為非原生 Enum 的 add_column 補 constraint)。顯式補 create_check_constraint 反而會 DuplicateObject。
- **BookingOverviewPage 過期註解**✅ `efce9c6`:兩處改為批次區間語義。
- **AdminSettingsPage 經費科目 index key**✅ `f2b1eb6`:列身分 key 以平行陣列維護,刪中間列不再焦點跳/IME 錯位。
- **結案照片上限未 gate 組態載入**✅ `9629d3f`:ActivityClosePage 比照 ActivityFormPage 以組態載入為前置(spin/error),移除前端 fallback 常數。

---

## 已知取捨(不變,需求方明確拍板,勿逕自改回)

- **儲存改實際磁碟空間、移除 capacity/reserve → 共機 Postgres DoS 風險**〔codex CRITICAL / opus MEDIUM〕:需求方明示「不設保留空間、之後自設容量告警、人為介入」。緩解:nginx `limit_conn uploads 4` + 上傳端點 256m 上限 + 其餘 1m。**待需求方的告警機制上線前,此為已接受風險**。

## 非本次 diff 範圍(base 既有,需求方核可一併修)

- ActivityFormPage 新建送審失敗再按送出重複建草稿 ✅ `9ade21d`:submit 失敗導 `/activities/:id/edit`,重試走更新。
- AppShell 手機側欄登出未 await ✅ `c08efb7`:比照桌面選單 `logout().then(navigate)`。

---

## 修復後驗證(2026-07-17,全綠)

- 後端:`pytest -q` **202 passed**(197 + 5 新測試)、`ruff check .` 全綠;migration `6b5a9c3affd0` up/down/up 於 dev 庫驗證過,回填值正確(UTC→台北跨日亦對)
- 前端:`tsc -b` 0 錯、`pnpm test` 35 passed、`pnpm run lint` 僅既有 fast-refresh warning
- dev 庫已 `reset_db + seed_mock`(固定借用帶當前學期起訖,場況圖可見)
- 註:`alembic downgrade base` 在含 seed 資料的庫上會在**第八輪舊 migration**(venues category CHECK 收窄)失敗,屬既有限制、與本輪無關
